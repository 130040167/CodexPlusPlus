# 2026-09-23 11:11 刷新事件

## 已证实

- 正在运行的 launcher 于 01:34:10 启动，磁盘 SHA256 为 `1645ABA05FD4BB005E405026035BB933166BF40959F3BC0423726E32A62CA40D`，匹配此前安装的修复版。
- 11:11:42 Crashpad 生成 renderer crash sidecar（`capture_kind=crash`、`ptype=renderer`）；新主窗口 renderer PID55440 同时启动。主进程 PID32352 保持运行。
- 主窗口 navigation timeOrigin 为 `1790133102722.1`，类型为 `reload`。Electron 原有 `render-process-gone` 监听器调用 `maybeRecoverFromRendererCrash`。
- 11:11:42.742 看门狗的 CDP 请求报 `Inspected target navigated or closed`；11:11:51.964 开始重注入，11:11:52 恢复 `/user-scripts/load`。因此这次恢复注入发生于崩溃之后。
- 启动至本次恢复有 **688 条可解析重注入记录**（其中687次发生在崩溃前，1次为崩溃后的恢复），generation达到689。08、09、10点分别146、144、141次。11:05–11:07约每25秒一次重注入，每次之后后端请求都立即成功；中间缺少预期每5秒的心跳。
- 原有日志存在交错写入导致的无效JSON行；计数仅针对可解析数据。新日志采用单进程文件写入，不混入原有日志。

## 尚未证实

- 无此次 native minidump 留存、无 native reason/exitCode、无崩溃前进程内存曲线，不能断言 OOM、GPU 或具体某段脚本是最终崩溃源。
- 当前代码按15秒心跳过期重注入；后台定时器暂停/节流可能解释25秒周期，但原日志没有可见性或实际定时器执行时间，仍需现场记录。
- 新 renderer 的私有内存观察到约1.9–2.4GB，而 JS used heap约220–256MB。进程私有内存并不等于 JS 堆，更不等于已证明内存泄漏。
- `localStorage` 普通未捕获异常不会自动证明 native renderer crash；重注入本身也没有无条件调用 `location.reload()`。此前对这两处因果关系的表述过强，应撤回。

## 与 PR #2241 的关系

连续重注入与 #2241 处理的生命周期/资源累积风险吻合。它的退避、陈旧会话退出、new-document script注销值得采纳，但合并前仍需验证退避不会被短暂注入宽限反复清零。当前诊断分支不合并其他PR，也不修改生产注入逻辑。

## 当前采集

独立 `record.mjs` 对已运行的本地调试端口采集，不重启应用。主进程钩子优先记录 `render-process-gone` 和 `exitCode`；CDP观察器记录主动导航原因、JS堆/DOM/监听器、页面可见性、注入代数和心跳时间。

日志在用户目录 `.codex-session-delete/renderer-diagnostics/`，源码在独立本地分支 `codex/local-renderer-diagnostics-20260923`。每15秒采样、24小时到期、每个日志文件上限16MiB。无远程提交、无日志上传。

回归通过：`cargo test -p codex-plus-core --test local_renderer_diagnostics`，模拟崩溃恢复顺序、安装幂等、容量限制和资源卸载。实际安装后已读到成功的主进程和renderer采样，页面timeOrigin未变化。尚未捕获下一次自然崩溃。

## 11:27 实际复现

观察器记录主页面11:26:43起为hidden；11:27:44最后成功心跳距采样16.7秒，generation仍为1；11:27:51.964启动器重注入；11:27:59页面仍hidden，generation升至2，timeOrigin始终未变化。11:28:47恢复visible时心跳重新保持新鲜。

这确认后台心跳延迟触发了重注入，且重注入本身不等于整页导航。没有故意隐藏、刷新或操控窗口；这些变化来自正常使用。该错误会反复增加注入负担，需独立修复。它与11:11 native crash之间的最终因果仍不能仅凭此证明。
